let myNumbers = [1, 2, 3, 4, 5];

// If we want to take out a number from the array:
myNumbers.splice(2, 1); // This tells the function to remove the number at the second index, and how MANY numbers to remove. It then becomes [1, 2, 4, 5]