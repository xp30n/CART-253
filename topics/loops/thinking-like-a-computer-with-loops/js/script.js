/**
 * Thinking like a computer with loops
 * Pippin Barr
 */

"use strict";

/**
 * Create a canvas
*/
function setup() {
    createCanvas(500, 500);
}


/**
 * Draw some loopy things
*/
function draw() {
    background(0);

     let x = 0;
     let y = heigjt / 2;
     let rectWidth = 50;
     fill(255, 0, 0);
     rect(x, y, rectWidth);
}